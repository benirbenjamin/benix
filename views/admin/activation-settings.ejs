<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Activation Settings - BenixSpace Admin</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-light">
  <%- include('../partials/navbar') %>

  <div class="container mt-4">
    <div class="row">
      <div class="col-md-8 mx-auto">
        <div class="card shadow-sm">
          <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
            <h5 class="mb-0">Account Activation Settings</h5>
          </div>
          <div class="card-body">
            <form action="/admin/activation-settings" method="POST">
              <div class="form-check form-switch mb-4">
                <input class="form-check-input" type="checkbox" id="is_activation_required" 
                       name="is_activation_required" <%= settings.is_activation_required ? 'checked' : '' %>>
                <label class="form-check-label" for="is_activation_required">
                  Require Account Activation
                </label>
                <div class="form-text">
                  When enabled, users must meet certain criteria before they can withdraw funds.
                </div>
              </div>

              <div class="mb-3">
                <label class="form-label">Minimum Shared Links Required</label>
                <input type="number" class="form-control" name="min_shared_links" 
                       value="<%= settings.min_shared_links %>" min="0" required>
                <div class="form-text">
                  Number of links a user must share before they can withdraw.
                </div>
              </div>

              <div class="mb-3">
                <label class="form-label">Minimum Login Days per Week</label>
                <input type="number" class="form-control" name="min_login_days" 
                       value="<%= settings.min_login_days %>" min="1" max="7" required>
                <div class="form-text">
                  Number of days a user must log in during the withdrawal week.
                </div>
              </div>

              <div class="mb-3">
                <label class="form-label">Minimum Clicks Before Withdraw</label>
                <input type="number" class="form-control" name="min_clicks_before_withdraw" 
                       value="<%= settings.min_clicks_before_withdraw %>" min="0" required>
                <div class="form-text">
                  Number of clicks required on shared links before a withdrawal can be made.
                </div>
              </div>

              <div class="text-end">
                <button type="submit" class="btn btn-primary">
                  <i class="fas fa-save me-2"></i>Save Settings
                </button>
              </div>
            </form>
          </div>
        </div>

        <div class="card shadow-sm mt-4">
          <div class="card-header bg-info text-white">
            <h5 class="mb-0">Current Activation Statistics</h5>
          </div>
          <div class="card-body">
            <div class="row g-3">
              <div class="col-md-4">
                <div class="border rounded p-3 text-center">
                  <h3><%= stats.totalUsers %></h3>
                  <small class="text-muted">Total Users</small>
                </div>
              </div>
              <div class="col-md-4">
                <div class="border rounded p-3 text-center">
                  <h3><%= stats.activeUsers %></h3>
                  <small class="text-muted">Active Users</small>
                </div>
              </div>
              <div class="col-md-4">
                <div class="border rounded p-3 text-center">
                  <h3><%= stats.pendingUsers %></h3>
                  <small class="text-muted">Pending Users</small>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>