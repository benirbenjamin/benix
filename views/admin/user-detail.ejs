<!DOCTYPE html>
<html lang="en">
<head>
  <title>User Details | Admin - BenixSpace</title>
  <link rel="icon" href="/static/img/favicon.png" type="image/png" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    .user-avatar {
      width: 120px;
      height: 120px;
      border-radius: 50%;
      background-color: #3b82f6;
      color: white;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 48px;
      margin-right: 20px;
    }
    .stat-card {
      transition: all 0.3s;
    }
    .stat-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 20px rgba(0,0,0,0.1);
    }
    .section-title {
      border-left: 4px solid #3b82f6;
      padding-left: 10px;
    }
    .contact-btn {
      background-color: #25D366;
      color: white;
    }
    .contact-btn:hover {
      background-color: #128C7E;
      color: white;
    }  </style>
</head>
<body class="bg-light">
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">BenixSpace</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto">
          <li class="nav-item">
            <a class="nav-link" href="/admin/dashboard">Dashboard</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="/admin/users">Users</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/admin/products">Products</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/admin/links">Links</a>
          </li>
        </ul>
        <ul class="navbar-nav">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= user.username %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <li><a class="dropdown-item" href="/admin/profile">My Profile</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item" href="/logout">Logout</a></li>
            </ul>
          </li>
        </ul>
      </div>
    </div>
  </nav>
  
  <div class="container-fluid py-4">
    <div class="row mb-4">
      <div class="col-lg-12">
        <nav aria-label="breadcrumb">
          <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/admin/dashboard">Dashboard</a></li>
            <li class="breadcrumb-item"><a href="/admin/users">Users</a></li>
            <li class="breadcrumb-item active" aria-current="page">User Details</li>
          </ol>
        </nav>
        
        <% if (success) { %>
          <div class="alert alert-success alert-dismissible fade show" role="alert">
            <%= success %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        <% } %>
        
        <% if (error) { %>
          <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <%= error %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        <% } %>
      </div>
    </div>

    <!-- User Profile Header -->
    <div class="row mb-4">
      <div class="col-lg-12">
        <div class="card shadow-sm">
          <div class="card-body">
            <div class="d-flex justify-content-between align-items-center">
              <div class="d-flex align-items-center">
                <div class="user-avatar">
                  <%= targetUser.username.charAt(0).toUpperCase() %>
                </div>
                <div>
                  <h2 class="mb-1"><%= targetUser.username %></h2>
                  <p class="text-muted mb-1"><%= targetUser.email %> · <span class="badge <%= targetUser.role === 'admin' ? 'bg-danger' : targetUser.role === 'merchant' ? 'bg-primary' : 'bg-success' %>"><%= targetUser.role %></span></p>
                  <p class="text-muted mb-1">
                    <% if (targetUser.phone_number) { %>
                      <i class="bi bi-telephone-fill me-1"></i> <%= targetUser.phone_number %> · 
                    <% } %>
                    <i class="bi bi-calendar3 me-1"></i> Joined: <%= new Date(targetUser.created_at).toLocaleDateString() %>
                    <% if (targetUser.has_lifetime_commission) { %>
                      · <span class="badge bg-warning text-dark"><i class="bi bi-star-fill me-1"></i>Premium</span>
                    <% } %>
                  </p>
                </div>
              </div>
              
              <div class="d-flex">
                <a href="https://wa.me/<%= targetUser.phone_number?.replace(/\+/g, '') || '' %>?text=Hello%20BenixSpace%20user" target="_blank" class="btn contact-btn me-2">
                  <i class="bi bi-whatsapp me-1"></i> WhatsApp
                </a>
                <div class="dropdown">
                  <button class="btn btn-outline-primary dropdown-toggle" type="button" id="userActions" data-bs-toggle="dropdown" aria-expanded="false">
                    Actions
                  </button>
                  <ul class="dropdown-menu" aria-labelledby="userActions">
                    <li><a class="dropdown-item" href="#" data-bs-toggle="modal" data-bs-target="#editUserModal"><i class="bi bi-pencil-square me-2"></i>Edit User</a></li>
                    <% if (targetUser.role === 'merchant') { %>
                      <li><a class="dropdown-item" href="/admin/merchants/<%= targetUser.id %>/edit"><i class="bi bi-shop me-2"></i>Edit Merchant Details</a></li>
                    <% } %>
                    <li><a class="dropdown-item" href="#" data-bs-toggle="modal" data-bs-target="#resetPasswordModal"><i class="bi bi-key me-2"></i>Reset Password</a></li>                      <% if (!targetUser.has_lifetime_commission) { %>
                      <li><a class="dropdown-item text-warning" href="#" onclick="togglePremiumStatus('<%= targetUser.id %>', true)"><i class="bi bi-star-fill me-2"></i>Upgrade to Premium</a></li>
                    <% } else { %>
                      <li><a class="dropdown-item text-warning" href="#" onclick="togglePremiumStatus('<%= targetUser.id %>', false)"><i class="bi bi-star me-2"></i>Remove Premium Status</a></li>
                    <% } %>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item text-danger" href="#" data-bs-toggle="modal" data-bs-target="#deleteUserModal"><i class="bi bi-trash me-2"></i>Delete User</a></li>
                  </ul>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Statistics Cards -->
    <div class="row mb-4">
      <div class="col-xl-3 col-md-6 mb-4">
        <div class="card border-left-primary shadow-sm h-100 py-2 stat-card">
          <div class="card-body">
            <div class="row no-gutters align-items-center">
              <div class="col mr-2">
                <div class="text-xs font-weight-bold text-primary text-uppercase mb-1">Wallet Balance</div>
                <div class="h5 mb-0 font-weight-bold text-gray-800">$<%= parseFloat(targetUser.wallet || 0).toFixed(4) %></div>
              </div>
              <div class="col-auto">
                <i class="bi bi-wallet2 fa-2x text-gray-300" style="font-size: 2rem;"></i>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="col-xl-3 col-md-6 mb-4">
        <div class="card border-left-success shadow-sm h-100 py-2 stat-card">
          <div class="card-body">
            <div class="row no-gutters align-items-center">
              <div class="col mr-2">
                <div class="text-xs font-weight-bold text-success text-uppercase mb-1">Total Earnings</div>
                <div class="h5 mb-0 font-weight-bold text-gray-800">$<%= parseFloat(targetUser.earnings || 0).toFixed(4) %></div>
              </div>
              <div class="col-auto">
                <i class="bi bi-cash-coin fa-2x text-gray-300" style="font-size: 2rem;"></i>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="col-xl-3 col-md-6 mb-4">
        <div class="card border-left-info shadow-sm h-100 py-2 stat-card">
          <div class="card-body">
            <div class="row no-gutters align-items-center">
              <div class="col mr-2">
                <div class="text-xs font-weight-bold text-info text-uppercase mb-1">Shared Links</div>
                <div class="h5 mb-0 font-weight-bold text-gray-800"><%= targetUser.total_shared_links || 0 %></div>
              </div>
              <div class="col-auto">
                <i class="bi bi-link-45deg fa-2x text-gray-300" style="font-size: 2rem;"></i>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="col-xl-3 col-md-6 mb-4">
        <div class="card border-left-warning shadow-sm h-100 py-2 stat-card">
          <div class="card-body">
            <div class="row no-gutters align-items-center">
              <div class="col mr-2">
                <div class="text-xs font-weight-bold text-warning text-uppercase mb-1">Total Orders</div>
                <div class="h5 mb-0 font-weight-bold text-gray-800"><%= targetUser.total_orders || 0 %></div>
              </div>
              <div class="col-auto">
                <i class="bi bi-bag-check fa-2x text-gray-300" style="font-size: 2rem;"></i>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Merchant-specific info -->
    <% if (targetUser.role === 'merchant') { %>
    <div class="row mb-4">
      <div class="col-lg-12">
        <div class="card shadow-sm">
          <div class="card-header bg-primary text-white">
            <h5 class="mb-0">Merchant Details</h5>
          </div>
          <div class="card-body">
            <div class="row">
              <div class="col-md-6">
                <h6 class="fw-bold">Business Name</h6>
                <p><%= targetUser.business_name || 'Not provided' %></p>
                
                <h6 class="fw-bold mt-3">Business Description</h6>
                <p><%= targetUser.business_description || 'Not provided' %></p>
              </div>
              <div class="col-md-6">
                <h6 class="fw-bold">Payment Information</h6>
                <table class="table table-sm">
                  <tr>
                    <th>Account Name</th>
                    <td><%= targetUser.account_name || 'Not provided' %></td>
                  </tr>
                  <tr>
                    <th>Account Number</th>
                    <td><%= targetUser.account_number || 'Not provided' %></td>
                  </tr>
                  <tr>
                    <th>Bank Code</th>
                    <td><%= targetUser.bank_code || 'Not provided' %></td>
                  </tr>
                </table>
                
                <div class="d-flex justify-content-between mt-3">
                  <div>
                    <h6 class="fw-bold">Amount Due</h6>
                    <p class="text-danger fw-bold">$<%= parseFloat(targetUser.amount_to_pay || 0).toFixed(4) %></p>
                  </div>
                  <div>
                    <h6 class="fw-bold">Paid Balance</h6>
                    <p class="text-success fw-bold">$<%= parseFloat(targetUser.paid_balance || 0).toFixed(4) %></p>
                  </div>
                  <div>                    <button class="btn btn-success btn-sm" onclick="markPaid('<%= targetUser.id %>')">
                      <i class="bi bi-check-circle me-1"></i> Mark as Paid
                    </button>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <% } %>

    <!-- User Details Tabs -->
    <div class="row">
      <div class="col-lg-12">
        <div class="card shadow-sm">
          <div class="card-header">
            <ul class="nav nav-tabs card-header-tabs" id="userDetailTabs" role="tablist">
              <li class="nav-item" role="presentation">
                <button class="nav-link active" id="profile-tab" data-bs-toggle="tab" data-bs-target="#profile" type="button" role="tab" aria-controls="profile" aria-selected="true">Profile</button>
              </li>
              <li class="nav-item" role="presentation">
                <button class="nav-link" id="orders-tab" data-bs-toggle="tab" data-bs-target="#orders" type="button" role="tab" aria-controls="orders" aria-selected="false">Orders</button>
              </li>
              <li class="nav-item" role="presentation">
                <button class="nav-link" id="shared-links-tab" data-bs-toggle="tab" data-bs-target="#shared-links" type="button" role="tab" aria-controls="shared-links" aria-selected="false">Shared Links</button>
              </li>
              <li class="nav-item" role="presentation">
                <button class="nav-link" id="transactions-tab" data-bs-toggle="tab" data-bs-target="#transactions" type="button" role="tab" aria-controls="transactions" aria-selected="false">Transactions</button>
              </li>
              <li class="nav-item" role="presentation">
                <button class="nav-link" id="referrals-tab" data-bs-toggle="tab" data-bs-target="#referrals" type="button" role="tab" aria-controls="referrals" aria-selected="false">Referrals</button>
              </li>
              <% if (targetUser.role === 'merchant') { %>
              <li class="nav-item" role="presentation">
                <button class="nav-link" id="merchant-analytics-tab" data-bs-toggle="tab" data-bs-target="#merchant-analytics" type="button" role="tab" aria-controls="merchant-analytics" aria-selected="false">Merchant Analytics</button>
              </li>
              <% } %>
            </ul>
          </div>
          <div class="card-body">
            <div class="tab-content" id="userDetailTabsContent">
              <!-- Profile Tab -->
              <div class="tab-pane fade show active" id="profile" role="tabpanel" aria-labelledby="profile-tab">
                <div class="row">
                  <div class="col-md-6">
                    <h5 class="section-title mb-4">Account Information</h5>
                    <table class="table">
                      <tr>
                        <th width="35%">User ID</th>
                        <td><%= targetUser.id %></td>
                      </tr>
                      <tr>
                        <th>Username</th>
                        <td><%= targetUser.username %></td>
                      </tr>
                      <tr>
                        <th>Email</th>
                        <td><%= targetUser.email %></td>
                      </tr>
                      <tr>
                        <th>Role</th>
                        <td><span class="badge <%= targetUser.role === 'admin' ? 'bg-danger' : targetUser.role === 'merchant' ? 'bg-primary' : 'bg-success' %>"><%= targetUser.role %></span></td>
                      </tr>
                      <tr>
                        <th>Status</th>
                        <td>
                          <span class="badge <%= 
                            targetUser.status === 'active' ? 'bg-success' : 
                            targetUser.status === 'pending' ? 'bg-warning' : 
                            targetUser.status === 'suspended' ? 'bg-secondary' : 
                            targetUser.status === 'banned' ? 'bg-danger' : 'bg-light text-dark' 
                          %>">
                            <%= targetUser.status || 'Unknown' %>
                          </span>
                        </td>
                      </tr>
                      <tr>
                        <th>Premium Status</th>
                        <td><%= targetUser.has_lifetime_commission ? 'Premium Member' : 'Standard Member' %></td>
                      </tr>
                      <tr>
                        <th>Registration Date</th>
                        <td><%= new Date(targetUser.created_at).toLocaleString() %></td>
                      </tr>
                      <tr>
                        <th>Last Login</th>
                        <td><%= targetUser.last_login_date ? new Date(targetUser.last_login_date).toLocaleString() : 'Never' %></td>
                      </tr>
                      <tr>
                        <th>Referral Code</th>
                        <td><%= targetUser.referral_id || 'None' %></td>
                      </tr>
                    </table>
                  </div>
                  <div class="col-md-6">
                    <h5 class="section-title mb-4">Contact Information</h5>
                    <table class="table">
                      <tr>
                        <th width="35%">Phone</th>
                        <td>
                          <% if (targetUser.phone_number) { %>
                            <%= targetUser.phone_number %>
                            <a href="tel:<%= targetUser.phone_number %>" class="btn btn-sm btn-outline-primary ms-2">
                              <i class="bi bi-telephone"></i> Call
                            </a>
                          <% } else { %>
                            Not provided
                          <% } %>
                        </td>
                      </tr>
                      <tr>
                        <th>Country</th>
                        <td><%= targetUser.country || 'Not provided' %></td>
                      </tr>
                      <tr>
                        <th>Account Name</th>
                        <td><%= targetUser.account_name || 'Not provided' %></td>
                      </tr>
                      <tr>
                        <th>Account Number</th>
                        <td><%= targetUser.account_number || 'Not provided' %></td>
                      </tr>
                      <tr>
                        <th>Bank Code</th>
                        <td><%= targetUser.bank_code || 'Not provided' %></td>
                      </tr>
                    </table>

                    <h5 class="section-title mt-4 mb-3">Admin Notes</h5>
                    <div class="card bg-light">
                      <div class="card-body">
                        <p class="card-text"><%= targetUser.notes || 'No notes recorded for this user.' %></p>
                        <button class="btn btn-sm btn-outline-primary" data-bs-toggle="modal" data-bs-target="#addNoteModal">
                          <i class="bi bi-pencil"></i> Edit Notes
                        </button>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
              
              <!-- Orders Tab -->
              <div class="tab-pane fade" id="orders" role="tabpanel" aria-labelledby="orders-tab">
                <% if (orders && orders.length > 0) { %>
                  <div class="table-responsive">
                    <table class="table table-hover">
                      <thead>
                        <tr>
                          <th>Order ID</th>
                          <th>Date</th>
                          <th>Items</th>
                          <th>Total</th>
                          <th>Status</th>
                          <th>Actions</th>
                        </tr>
                      </thead>
                      <tbody>
                        <% orders.forEach(function(order) { %>
                          <tr>
                            <td>#<%= order.id %></td>
                            <td><%= new Date(order.created_at).toLocaleString() %></td>
                            <td><%= order.item_count %></td>
                            <td>$<%= parseFloat(order.total_amount).toFixed(4) %></td>
                            <td>
                              <span class="badge <%= 
                                order.status === 'completed' ? 'bg-success' : 
                                order.status === 'processing' ? 'bg-info' : 
                                order.status === 'shipped' ? 'bg-primary' : 
                                order.status === 'delivered' ? 'bg-success' : 
                                order.status === 'cancelled' ? 'bg-danger' : 'bg-warning' 
                              %>">
                                <%= order.status %>
                              </span>
                            </td>
                            <td>
                              <a href="/admin/orders/<%= order.id %>" class="btn btn-sm btn-outline-primary">View</a>
                            </td>
                          </tr>
                        <% }); %>
                      </tbody>
                    </table>
                  </div>
                  <% if (targetUser.total_orders > orders.length) { %>
                    <div class="text-center mt-3">
                      <a href="/admin/orders?user=<%= targetUser.id %>" class="btn btn-outline-primary btn-sm">
                        View All Orders (<%= targetUser.total_orders %>)
                      </a>
                    </div>
                  <% } %>
                <% } else { %>
                  <div class="text-center py-5">
                    <i class="bi bi-bag-x" style="font-size: 3rem; color: #adb5bd;"></i>
                    <p class="mt-3">This user has not made any orders yet.</p>
                  </div>
                <% } %>
              </div>
              
              <!-- Shared Links Tab -->
              <div class="tab-pane fade" id="shared-links" role="tabpanel" aria-labelledby="shared-links-tab">
                <% if (sharedLinks && sharedLinks.length > 0) { %>
                  <div class="table-responsive">
                    <table class="table table-hover">
                      <thead>
                        <tr>
                          <th>Link</th>
                          <th>Merchant</th>
                          <th>Type</th>
                          <th>Clicks</th>
                          <th>Earnings</th>
                          <th>Actions</th>
                        </tr>
                      </thead>
                      <tbody>
                        <% sharedLinks.forEach(function(link) { %>
                          <tr>
                            <td>
                              <a href="<%= link.url %>" target="_blank"><%= link.title %></a>
                              <small class="d-block text-muted">Code: <%= link.share_code %></small>
                            </td>
                            <td><%= link.merchant_name %></td>
                            <td><span class="badge bg-info"><%= link.type %></span></td>
                            <td><%= link.clicks %></td>
                            <td>$<%= parseFloat(link.earnings).toFixed(4) %></td>
                            <td>
                              <div class="btn-group btn-group-sm">
                                <a href="/l/<%= link.share_code %>" target="_blank" class="btn btn-outline-primary">Visit</a>
                                <a href="/admin/links/<%= link.link_id %>" class="btn btn-outline-secondary">View Original</a>
                              </div>
                            </td>
                          </tr>
                        <% }); %>
                      </tbody>
                    </table>
                  </div>
                <% } else { %>
                  <div class="text-center py-5">
                    <i class="bi bi-link-45deg" style="font-size: 3rem; color: #adb5bd;"></i>
                    <p class="mt-3">This user has not shared any links yet.</p>
                  </div>
                <% } %>
              </div>
              
              <!-- Transactions Tab -->
              <div class="tab-pane fade" id="transactions" role="tabpanel" aria-labelledby="transactions-tab">
                <% if (transactions && transactions.length > 0) { %>
                  <div class="table-responsive">
                    <table class="table table-hover">
                      <thead>
                        <tr>
                          <th>ID</th>
                          <th>Date</th>
                          <th>Type</th>
                          <th>Amount</th>
                          <th>Status</th>
                          <th>Details</th>
                        </tr>
                      </thead>
                      <tbody>
                        <% transactions.forEach(function(tx) { %>
                          <tr>
                            <td>#<%= tx.id %></td>
                            <td><%= new Date(tx.created_at).toLocaleString() %></td>
                            <td>
                              <span class="badge <%= 
                                tx.type === 'deposit' ? 'bg-success' : 
                                tx.type === 'withdrawal' ? 'bg-warning' : 
                                tx.type === 'commission' ? 'bg-info' : 
                                tx.type === 'upgrade' ? 'bg-primary' : 'bg-secondary' 
                              %>">
                                <%= tx.type %>
                              </span>
                            </td>
                            <td class="<%= parseFloat(tx.amount) >= 0 ? 'text-success' : 'text-danger' %>">
                              $<%= parseFloat(tx.amount).toFixed(4) %>
                            </td>
                            <td>
                              <span class="badge <%= 
                                tx.status === 'completed' ? 'bg-success' : 
                                tx.status === 'pending' ? 'bg-warning' : 'bg-danger' 
                              %>">
                                <%= tx.status %>
                              </span>
                            </td>
                            <td>
                              <button class="btn btn-sm btn-outline-info" onclick="showTransactionDetails('<%= tx.details %>', '<%= tx.notes %>')">
                                View
                              </button>
                            </td>
                          </tr>
                        <% }); %>
                      </tbody>
                    </table>
                  </div>
                <% } else { %>
                  <div class="text-center py-5">
                    <i class="bi bi-credit-card" style="font-size: 3rem; color: #adb5bd;"></i>
                    <p class="mt-3">This user has no transaction history yet.</p>
                  </div>
                <% } %>
              </div>
              
              <!-- Referrals Tab -->
              <div class="tab-pane fade" id="referrals" role="tabpanel" aria-labelledby="referrals-tab">
                <% if (referrals && referrals.length > 0) { %>
                  <div class="table-responsive">
                    <table class="table table-hover">
                      <thead>
                        <tr>
                          <th>Referred User</th>
                          <th>Email</th>
                          <th>Date Referred</th>
                          <th>Status</th>
                          <th>Activated Date</th>
                          <th>Actions</th>
                        </tr>
                      </thead>
                      <tbody>
                        <% referrals.forEach(function(referral) { %>
                          <tr>
                            <td><%= referral.referred_username %></td>
                            <td><%= referral.referred_email %></td>
                            <td><%= new Date(referral.created_at).toLocaleString() %></td>
                            <td>
                              <span class="badge <%= 
                                referral.status === 'activated' ? 'bg-success' : 
                                referral.status === 'pending' ? 'bg-warning' : 
                                referral.status === 'expired' ? 'bg-danger' : 'bg-secondary' 
                              %>">
                                <%= referral.status %>
                              </span>
                            </td>
                            <td><%= referral.activated_at ? new Date(referral.activated_at).toLocaleString() : 'Not activated' %></td>
                            <td>
                              <a href="/admin/users/<%= referral.referred_id %>" class="btn btn-sm btn-outline-primary">View User</a>
                            </td>
                          </tr>
                        <% }); %>
                      </tbody>
                    </table>
                  </div>
                <% } else { %>
                  <div class="text-center py-5">
                    <i class="bi bi-people" style="font-size: 3rem; color: #adb5bd;"></i>
                    <p class="mt-3">This user hasn't referred anyone yet.</p>
                  </div>
                <% } %>

                <% if (targetUser.referral_id) { %>
                  <div class="card mt-4">
                    <div class="card-body">
                      <h6 class="card-title">User's Referral Link</h6>
                      <div class="input-group">
                        <input type="text" class="form-control" id="referralLink" value="<%= baseUrl %>/register?ref=<%= targetUser.referral_id %>" readonly>
                        <button class="btn btn-outline-primary" type="button" onclick="copyReferralLink()">Copy</button>
                      </div>
                    </div>
                  </div>
                <% } %>
              </div>

              <!-- Merchant Analytics Tab -->
              <% if (targetUser.role === 'merchant') { %>
              <div class="tab-pane fade" id="merchant-analytics" role="tabpanel" aria-labelledby="merchant-analytics-tab">
                <% if (merchantAnalytics) { %>
                  <!-- Analytics Summary -->
                  <div class="row mb-4">
                    <div class="col-md-3">
                      <div class="card bg-primary text-white">
                        <div class="card-body text-center">
                          <h4><%= merchantAnalytics.totalStats.total_links %></h4>
                          <small>Total Links</small>
                        </div>
                      </div>
                    </div>
                    <div class="col-md-3">
                      <div class="card bg-info text-white">
                        <div class="card-body text-center">
                          <h4><%= merchantAnalytics.totalStats.total_shares %></h4>
                          <small>Total Shares</small>
                        </div>
                      </div>
                    </div>
                    <div class="col-md-3">
                      <div class="card bg-success text-white">
                        <div class="card-body text-center">
                          <h4><%= merchantAnalytics.totalStats.total_clicks %></h4>
                          <small>Total Clicks</small>
                        </div>
                      </div>
                    </div>
                    <div class="col-md-3">
                      <div class="card bg-warning text-dark">
                        <div class="card-body text-center">
                          <h4>$<%= parseFloat(merchantAnalytics.totalStats.total_commissions_paid).toFixed(4) %></h4>
                          <small>Commissions Paid</small>
                        </div>
                      </div>
                    </div>
                  </div>

                  <!-- Quick Actions -->
                  <div class="mb-4">
                    <a href="/admin/merchant-analytics/<%= targetUser.id %>" class="btn btn-primary">
                      <i class="bi bi-graph-up"></i> View Detailed Analytics
                    </a>
                  </div>

                  <!-- Top Performing Links -->
                  <div class="card mb-4">
                    <div class="card-header">
                      <h6 class="mb-0">Top Performing Links</h6>
                    </div>
                    <div class="card-body">
                      <% if (merchantAnalytics.linkAnalytics && merchantAnalytics.linkAnalytics.length > 0) { %>
                        <div class="table-responsive">
                          <table class="table table-sm">
                            <thead>
                              <tr>
                                <th>Link Title</th>
                                <th>Type</th>
                                <th>Shares</th>
                                <th>Clicks</th>
                                <th>Cost Incurred</th>
                              </tr>
                            </thead>
                            <tbody>
                              <% merchantAnalytics.linkAnalytics.slice(0, 5).forEach(link => { %>
                                <tr>
                                  <td>
                                    <strong><%= link.title %></strong>
                                    <br><small class="text-muted">CPC: $<%= parseFloat(link.cost_per_click).toFixed(4) %></small>
                                  </td>
                                  <td>
                                    <span class="badge bg-<%= link.type === 'product' ? 'primary' : 'info' %>">
                                      <%= link.type %>
                                    </span>
                                  </td>
                                  <td><%= link.total_shares %></td>
                                  <td><span class="badge bg-success"><%= link.total_clicks %></span></td>
                                  <td class="text-danger">$<%= parseFloat(link.total_cost_incurred).toFixed(4) %></td>
                                </tr>
                              <% }) %>
                            </tbody>
                          </table>
                        </div>
                      <% } else { %>
                        <div class="text-center py-3">
                          <i class="bi bi-link-45deg text-muted" style="font-size: 2rem;"></i>
                          <p class="text-muted mt-2">No links created yet</p>
                        </div>
                      <% } %>
                    </div>
                  </div>

                  <!-- Recent Monthly Performance -->
                  <div class="card">
                    <div class="card-header">
                      <h6 class="mb-0">Recent Monthly Performance</h6>
                    </div>
                    <div class="card-body">
                      <% if (merchantAnalytics.monthlyStats && merchantAnalytics.monthlyStats.length > 0) { %>
                        <div class="table-responsive">
                          <table class="table table-sm">
                            <thead>
                              <tr>
                                <th>Month</th>
                                <th>Clicks</th>
                                <th>Commissions Paid</th>
                              </tr>
                            </thead>
                            <tbody>
                              <% merchantAnalytics.monthlyStats.forEach(month => { %>
                                <tr>
                                  <td><%= month.month %></td>
                                  <td><%= month.clicks %></td>
                                  <td class="text-success">$<%= parseFloat(month.commissions_paid || 0).toFixed(4) %></td>
                                </tr>
                              <% }) %>
                            </tbody>
                          </table>
                        </div>
                      <% } else { %>
                        <div class="text-center py-3">
                          <i class="bi bi-calendar-x text-muted" style="font-size: 2rem;"></i>
                          <p class="text-muted mt-2">No monthly data available</p>
                        </div>
                      <% } %>
                    </div>
                  </div>

                <% } else { %>
                  <div class="text-center py-5">
                    <i class="bi bi-graph-up text-muted" style="font-size: 3rem;"></i>
                    <p class="text-muted mt-3">Analytics data not available</p>
                  </div>
                <% } %>
              </div>
              <% } %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Modals -->
  <!-- Edit User Modal -->
  <div class="modal fade" id="editUserModal" tabindex="-1" aria-labelledby="editUserModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="editUserModalLabel">Edit User</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <form id="editUserForm" action="/admin/users/<%= targetUser.id %>/update" method="POST">
            <div class="mb-3">
              <label for="username" class="form-label">Username</label>
              <input type="text" class="form-control" id="username" name="username" value="<%= targetUser.username %>" required>
            </div>
            <div class="mb-3">
              <label for="email" class="form-label">Email</label>
              <input type="email" class="form-control" id="email" name="email" value="<%= targetUser.email %>" required>
            </div>
            <div class="mb-3">
              <label for="role" class="form-label">Role</label>
              <select class="form-select" id="role" name="role" required>
                <option value="user" <%= targetUser.role === 'user' ? 'selected' : '' %>>User</option>
                <option value="merchant" <%= targetUser.role === 'merchant' ? 'selected' : '' %>>Merchant</option>
                <option value="admin" <%= targetUser.role === 'admin' ? 'selected' : '' %>>Admin</option>
              </select>
            </div>
            <div class="mb-3">
              <label for="wallet" class="form-label">Wallet Balance</label>
              <div class="input-group">
                <span class="input-group-text">$</span>
                <input type="number" class="form-control" id="wallet" name="wallet" step="0.01" value="<%= parseFloat(targetUser.wallet || 0).toFixed(4) %>">
              </div>
            </div>
            <div class="mb-3 form-check">
              <input type="checkbox" class="form-check-input" id="has_lifetime_commission" name="has_lifetime_commission" <%= targetUser.has_lifetime_commission ? 'checked' : '' %>>
              <label class="form-check-label" for="has_lifetime_commission">Premium User (Lifetime Commission)</label>
            </div>
          </form>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
          <button type="button" class="btn btn-primary" onclick="submitUserEdit()">Save Changes</button>
        </div>
      </div>
    </div>
  </div>

  <!-- Reset Password Modal -->
  <div class="modal fade" id="resetPasswordModal" tabindex="-1" aria-labelledby="resetPasswordModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="resetPasswordModalLabel">Reset Password</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <!-- Error display area -->
          <div id="resetPasswordError" class="alert alert-danger d-none mb-3">
            <i class="bi bi-exclamation-triangle-fill me-2"></i>
            <span id="resetPasswordErrorMessage"></span>
          </div>
          
          <!-- Success display area -->
          <div id="resetPasswordSuccess" class="alert alert-success d-none mb-3">
            <i class="bi bi-check-circle-fill me-2"></i>
            <span id="resetPasswordSuccessMessage"></span>
          </div>
          
          <div class="mb-3">
            <label for="new_password" class="form-label">New Password *</label>
            <input type="password" class="form-control" id="new_password" minlength="8" required 
                   onkeypress="if(event.key==='Enter'){event.preventDefault();}">
            <div class="form-text">Password must be at least 8 characters long</div>
          </div>
          <div class="mb-3">
            <label for="confirm_password" class="form-label">Confirm Password *</label>
            <input type="password" class="form-control" id="confirm_password" minlength="8" required
                   onkeypress="if(event.key==='Enter'){event.preventDefault();}">
            <div class="form-text">Re-enter the password to confirm</div>
          </div>
          <div class="alert alert-warning">
            <i class="bi bi-exclamation-triangle-fill me-2"></i> This action will reset the user's password. The new password will be effective immediately.
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
          <button type="button" class="btn btn-primary" onclick="resetPassword('<%= targetUser.id %>')">Reset Password</button>
        </div>
      </div>
    </div>
  </div>

  <!-- Add Note Modal -->
  <div class="modal fade" id="addNoteModal" tabindex="-1" aria-labelledby="addNoteModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="addNoteModalLabel">Edit Admin Notes</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <div class="mb-3">
            <label for="admin_notes" class="form-label">Notes</label>
            <textarea class="form-control" id="admin_notes" rows="5"><%= targetUser.notes || '' %></textarea>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
          <button type="button" class="btn btn-primary" onclick="saveNotes('<%= targetUser.id %>')">Save Notes</button>
        </div>
      </div>
    </div>
  </div>

  <!-- Delete User Modal -->
  <div class="modal fade" id="deleteUserModal" tabindex="-1" aria-labelledby="deleteUserModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="deleteUserModalLabel">Delete User</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <div class="alert alert-danger">
            <i class="bi bi-exclamation-triangle-fill me-2"></i> Are you sure you want to delete this user? This action cannot be undone.
          </div>
          <p>User: <strong><%= targetUser.username %></strong></p>
          <p>Email: <strong><%= targetUser.email %></strong></p>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
          <button type="button" class="btn btn-danger" onclick="deleteUser('<%= targetUser.id %>')">Delete User</button>
        </div>
      </div>
    </div>
  </div>
  
  <!-- Transaction Details Modal -->
  <div class="modal fade" id="txDetailsModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">Transaction Details</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <div class="mb-3">
            <h6>Details</h6>
            <p id="txDetailsText"></p>
          </div>
          <div class="mb-3">
            <h6>Notes</h6>
            <p id="txNotesText" class="text-muted"></p>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
        </div>
      </div>
    </div>
  </div>
  
  <script>
    function submitUserEdit() {
      document.getElementById('editUserForm').submit();
    }
    
    // Helper functions for showing/hiding messages in modal
    function showResetPasswordError(message) {
      const errorDiv = document.getElementById('resetPasswordError');
      const errorMessage = document.getElementById('resetPasswordErrorMessage');
      errorMessage.textContent = message;
      errorDiv.classList.remove('d-none');
      // Hide success message
      document.getElementById('resetPasswordSuccess').classList.add('d-none');
    }

    function showResetPasswordSuccess(message) {
      const successDiv = document.getElementById('resetPasswordSuccess');
      const successMessage = document.getElementById('resetPasswordSuccessMessage');
      successMessage.textContent = message;
      successDiv.classList.remove('d-none');
      // Hide error message
      document.getElementById('resetPasswordError').classList.add('d-none');
    }

    function hideResetPasswordMessages() {
      document.getElementById('resetPasswordError').classList.add('d-none');
      document.getElementById('resetPasswordSuccess').classList.add('d-none');
    }
    
    // Global flag to prevent double submission
    let isResettingPassword = false;
    
    function resetPassword(userId) {
      // Prevent double submission
      if (isResettingPassword) {
        console.log('Password reset already in progress, ignoring duplicate call');
        return;
      }
      
      const newPassword = document.getElementById('new_password').value;
      const confirmPassword = document.getElementById('confirm_password').value;
      
      console.log('Password reset initiated:', { 
        userId, 
        hasNewPassword: !!newPassword, 
        hasConfirmPassword: !!confirmPassword,
        newPasswordLength: newPassword ? newPassword.length : 0 
      });
      
      // Hide previous messages
      hideResetPasswordMessages();
      
      if (!newPassword || !confirmPassword) {
        showResetPasswordError('Please fill in all fields');
        return;
      }
      
      // Validate password length
      if (newPassword.length < 8) {
        showResetPasswordError('Password must be at least 8 characters long');
        return;
      }
      
      // Validate passwords match
      if (newPassword !== confirmPassword) {
        showResetPasswordError('Passwords do not match');
        return;
      }
      
      // Set global flag to prevent double submission
      isResettingPassword = true;
      
      // Show loading state and disable button
      const resetBtn = document.querySelector('#resetPasswordModal .btn-primary');
      const originalText = resetBtn.innerHTML;
      resetBtn.innerHTML = '<i class="bi bi-hourglass-split"></i> Resetting...';
      resetBtn.disabled = true;
      
      fetch(`/admin/users/${userId}/set-password`, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'X-Requested-With': 'XMLHttpRequest'
        },
        body: JSON.stringify({ new_password: newPassword }),
      })
      .then(response => {
        if (!response.ok) {
          throw new Error(`HTTP error! status: ${response.status}`);
        }
        return response.json();
      })
      .then(data => {
        if (data.success) {
          showResetPasswordSuccess('Password has been reset successfully!');
          // Clear form
          document.getElementById('new_password').value = '';
          document.getElementById('confirm_password').value = '';
          // Close modal after 2 seconds
          setTimeout(() => {
            const modal = bootstrap.Modal.getInstance(document.getElementById('resetPasswordModal'));
            if (modal) {
              modal.hide();
            }
          }, 2000);
        } else {
          showResetPasswordError(data.message || 'Failed to reset password');
        }
      })
      .catch(error => {
        console.error('Error:', error);
        showResetPasswordError('An error occurred while resetting password. Please try again.');
      })
      .finally(() => {
        // Restore button state and reset global flag
        resetBtn.innerHTML = originalText;
        resetBtn.disabled = false;
        isResettingPassword = false;
      });
    }
    
    // Clear form when modal opens
    document.addEventListener('DOMContentLoaded', function() {
      const resetModal = document.getElementById('resetPasswordModal');
      if (resetModal) {
        resetModal.addEventListener('show.bs.modal', function() {
          // Clear form fields
          document.getElementById('new_password').value = '';
          document.getElementById('confirm_password').value = '';
          // Hide any previous messages
          hideResetPasswordMessages();
          // Reset global flag in case of modal reopening
          isResettingPassword = false;
        });
      }
    });
    
    function togglePremiumStatus(userId, status) {
      if (!confirm(status ? 
        'Are you sure you want to upgrade this user to premium?' : 
        'Are you sure you want to remove premium status from this user?')) {
        return;
      }
      
      fetch(`/admin/users/${userId}/toggle-premium`, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        }
      })
      .then(response => response.json())
      .then(data => {
        if (data.success) {
          alert(data.message);
          location.reload();
        } else {
          alert(data.message);
        }
      })
      .catch(error => {
        console.error('Error:', error);
        alert('An error occurred while updating user status.');
      });
    }
    
    function markPaid(userId) {
      if (!confirm('Are you sure you want to mark this merchant as paid?')) {
        return;
      }
      
      fetch(`/admin/merchants/${userId}/mark-paid`, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        }
      })
      .then(response => response.json())
      .then(data => {
        if (data.success) {
          alert(data.message);
          location.reload();
        } else {
          alert(data.message);
        }
      })
      .catch(error => {
        console.error('Error:', error);
        alert('An error occurred while processing payment.');
      });
    }
    
    function saveNotes(userId) {
      const notes = document.getElementById('admin_notes').value;
      
      fetch(`/admin/users/${userId}/update-notes`, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({ notes }),
      })
      .then(response => response.json())
      .then(data => {
        if (data.success) {
          alert('Notes updated successfully');
          $('#addNoteModal').modal('hide');
          location.reload();
        } else {
          alert(data.message || 'Error updating notes');
        }
      })
      .catch(error => {
        console.error('Error:', error);
        alert('An error occurred while saving notes.');
      });
    }
    
    function deleteUser(userId) {
      fetch(`/admin/users/${userId}/delete`, {
        method: 'DELETE',
        headers: {
          'Content-Type': 'application/json',
        }
      })
      .then(response => response.json())
      .then(data => {
        if (data.success) {
          alert(data.message);
          window.location.href = '/admin/users';
        } else {
          alert(data.message);
        }
      })
      .catch(error => {
        console.error('Error:', error);
        alert('An error occurred while deleting user.');
      });
    }
    
    function showTransactionDetails(details, notes) {
      document.getElementById('txDetailsText').innerText = details || 'No details provided';
      document.getElementById('txNotesText').innerText = notes || 'No notes available';
      new bootstrap.Modal(document.getElementById('txDetailsModal')).show();
    }
    
    function copyReferralLink() {
      const copyText = document.getElementById("referralLink");
      copyText.select();
      document.execCommand("copy");
      alert("Referral link copied to clipboard!");
    }  </script>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>